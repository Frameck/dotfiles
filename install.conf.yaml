- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config:

- create:
    - ~/Code
    - ~/.vim/undo-history

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true