- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Code
    - ~/.vim/undo-history

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true